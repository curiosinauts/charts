---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-path-pvc-{{.Release.Name}}
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: {{.Values.storageSize}}